function mostrar()
{

  var contador=0;
  var numeroMaximo
  var numeroMinimo
  var bandera =true

	// declarar variables

	var respuesta='si';

  while (confirm ()){
 do{
 numero = prompt ("ingrese un numero")
 numero = parseInt (num)}
 while (isNaN(num));
  if (bandera){
  bandera = false;
  numeroMaximo = numero;
  numeroMinimo = numero;
}else{
  if (numero> numeroMaximo){
  numeroMaximo = numero
  }
  if (numero < numeroMinimo){
  numeroMinimo = numero
  }










}//FIN DE LA FUNCIÓN
